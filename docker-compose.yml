version: '3.8'

services:
  targetmind:
    build: .
    container_name: targetmind-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
    environment:
      - PORT=8000
